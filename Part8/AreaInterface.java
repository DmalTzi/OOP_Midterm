interface AreaCir {
    public float area(float r);
}

interface AreaSqu {
    public float area(float w);
}

interface AreaRec {
    public float area(float w, float h);
}

interface AreaTri {
    public float area(float b, float h);
}